# LangChain Google GenAI 项目环境配置文件示例
# 复制此文件为 .env 并填写你的实际配置

# LangSmith 追踪（可选）
LANGSMITH_TRACING="true"
LANGSMITH_API_KEY="你的_LANGSMITH_API_KEY"

# Google Generative AI API Key (必需)
GOOGLE_API_KEY=你的_GOOGLE_API_KEY

# ChromaDB 配置
# 设置为 true 启用 ChromaDB 持久性存储，false 使用内存存储
USE_CHROMA=false
# ChromaDB 服务地址（默认本地8000端口）
CHROMA_URL=http://localhost:8000

# ====== ChromaDB 使用说明 ======
# 1. 启动 ChromaDB 服务 (在8000端口):
#    docker run -p 8000:8000 chromadb/chroma
#    或者: pip install chromadb && python -m chromadb.cli run --host localhost --port 8000
#
# 2. 设置 USE_CHROMA=true 启用持久性存储
#
# 3. 可选：修改 CHROMA_URL 指向你的 ChromaDB 服务地址
#
# 优势：
# - 向量数据持久化存储
# - 重启应用后数据不丢失
# - 支持大规模数据集
# - 更好的查询性能